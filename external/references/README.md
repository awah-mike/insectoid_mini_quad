# Reference Material

Downloaded reference projects, archives, and comparison rollouts.

This directory is ignored by Git except for this README. Promote only small,
project-owned derivative data into `data/` when a script depends on it.
